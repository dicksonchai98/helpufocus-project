import { CODE_TOKEN_INVALID_ERROR } from './errorCode';

export default {
  statusCode: 401,
  statusMessage: 'Token invalid error.',
  data: {
    errorCode: CODE_TOKEN_INVALID_ERROR,
    errorMessage: 'Token invalid error.'
  }
}
