import { CODE_RESOURCE_INVALID_ERROR } from './errorCode';

export default {
  statusCode: 404,
  statusMessage: 'Resource is invalid.',
  data: {
    errorCode: CODE_RESOURCE_INVALID_ERROR,
    errorMessage: 'Resource is invalid.'
  }
}
